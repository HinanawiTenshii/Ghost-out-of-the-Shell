using UnityEngine;

/// <summary>
/// Attach to an object that must remain active even while it is outside the
/// camera's circular vision mask.
/// </summary>
public sealed class CameraVisionStreamingExempt : MonoBehaviour
{
}
